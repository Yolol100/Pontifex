name: WordPress compatibility

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  runtime:
    name: WordPress ${{ matrix.wordpress }} / PHP ${{ matrix.php }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: wordpress
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    strategy:
      fail-fast: false
      matrix:
        include:
          - wordpress: 6.0
            php: '8.1'
          - wordpress: 7.1
            php: '8.3'

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          extensions: mysqli, soap
          tools: composer:v2

      - name: Install and audit locked Composer dependencies
        shell: bash
        run: |
          set -euo pipefail
          composer validate --no-check-publish
          composer install --no-interaction --no-progress --prefer-dist
          composer audit --locked

      - name: PHP syntax check
        shell: bash
        run: |
          set -euo pipefail
          find . -type f -name '*.php' -not -path './.git/*' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l

      - name: Install verified WP-CLI 2.12.0
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --location --silent --show-error --output wp-cli.phar \
            https://github.com/wp-cli/wp-cli/releases/download/v2.12.0/wp-cli-2.12.0.phar
          echo 'ce34ddd838f7351d6759068d09793f26755463b4a4610a5a5c0a97b68220d85c  wp-cli.phar' | sha256sum --check --strict

      - name: Create clean WordPress runtime and activate plugin
        shell: bash
        run: |
          set -euo pipefail
          runtime=/tmp/pontifex-runtime
          rm -rf "$runtime"
          php wp-cli.phar core download --path="$runtime" --version='${{ matrix.wordpress }}' --locale=en_US --force
          php wp-cli.phar config create --path="$runtime" --dbname=wordpress --dbuser=root --dbpass=root --dbhost=127.0.0.1:3306
          php wp-cli.phar config set WP_DEBUG true --raw --path="$runtime"
          php wp-cli.phar config set WP_DEBUG_LOG true --raw --path="$runtime"
          php wp-cli.phar core install \
            --path="$runtime" \
            --url=http://pontifex.test \
            --title='Pontifex Runtime' \
            --admin_user=admin \
            --admin_password='runtime-only-password' \
            --admin_email=runtime@example.invalid \
            --skip-email
          rm -f "$runtime/wp-content/debug.log"
          ln -s "$GITHUB_WORKSPACE" "$runtime/wp-content/plugins/pontifex-oi"
          php wp-cli.phar plugin activate pontifex-oi --path="$runtime"
          php wp-cli.phar plugin status pontifex-oi --path="$runtime"
          php wp-cli.phar eval 'echo defined("PONTIFEX_OI_VERSION") ? PONTIFEX_OI_VERSION . "\n" : "missing\n";' --path="$runtime"

      - name: Reject runtime PHP errors
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          log=/tmp/pontifex-runtime/wp-content/debug.log
          if [[ -f "$log" ]] && grep -Eqi 'PHP (Fatal error|Parse error|Warning|Deprecated|Notice)' "$log"; then
            cat "$log"
            exit 1
          fi
